--------------------------------------------
-------------- v3.2.0.ConSys.1 -------------
--------------------------------------------

Add first integral constraint to enable continuation in conversative systems. Changes affect:

    @Continuation:
        - Continuation:
            --> Constructor: Added DYN to input arguments (line 100)
            --> Constructor: If the DYN.system field 'first_integral' exists, the fsolve algorithm is set to Levenberg-Marquardt (lines 113 - 115)
        - initial_slope:
            --> Use of variable "newtonOpts" disabled, fsolve options from CON object are used (lines 8, 14, 16, 22)
        - m_continuation:
            --> Added re-computation and check of residuum after fsolve. A residuum >= 1e-10 is treated like fsolve exitflag < 1 or = 2 (lines 95, 98, 99, 107 - 110, 119 and 125 - 128)

    @DynamicalSystem:
        - DynamicalSystem:
            --> Added the property first_integral as data type "function_handle" (line 22)
            --> Temporarily disabled the log (line 55)
        - s_DYN_gatekeeper:
            --> Added the field 'first_integral' to "system_allowed_fieldnames" (line 13)
            --> Added check for correct data type for field 'first_integral' and moved the check for field 'info' one line below (lines 47 and 48)

    @AM_PS_FDM:
        - PS_FDM_residuum:
            --> Calculation of the first integral constraint added (lines 91 - 98)
            --> Assembly of residuum restructured (former lines 77 - 90 and 89 removed, lines 77 - 78, 87 - 89 and 100 - 101 added)
            --> Calculation of dIC/dy added (lines 166 - 182)
            --> Assembly of Jacobian restructured (former lines 141 - 144 and 152 - 153 removed, lines 152 - 153, 161 - 164 and 184 - 187 added)

    @AM_PS_FGM:
        - PS_FGM_residuum:
            --> If the DYN.system field 'first_integral' exists, the residuum is expanded by the first integral constraint (lines 67 - 73)

    @AM_PS_SHM:
        - PS_SHM_residuum:
            --> Integration with perturbed mu-value wrapped into an if-condition: Integration is not necessary if value of first integral is the continuation parameter (lines 107 and 117)
            --> Calculation of the first integral constraint added (lines 140 - 147)
            --> Assembly of residuum restructured (former lines 123 - 125 and 135 - 137 removed, lines 126, 136 - 138 and 149 added)
            --> Calculation of dg/dmu wrapped into an if-condition: It is zero if value of first integral is the continuation parameter (lines 178 - 180 and 186 added)
            --> Calculation of dpc/dmu wrapped into an if-condition: It is zero if value of first integral is the continuation parameter (lines 266 - 268 and 279 added)
            --> Calculation of dIC/dy added (lines 288 - 310)
            --> Assembly of Jacobian restructured (former lines 265 - 270 removed, lines 312 - 315 added)

    @SOL_PS_SHM:
        - SOL_PS_FDM:
            --> Changed the initialisation of obj.dy from "NaN(size(J1,1),1)" to "NaN(size(y1))" since size(J1,1) = size(obj.dy,1) + 1 for conservative systems (line 37)
        - SOL_PS_FGM:
            --> Changed the initialisation of obj.dy from "NaN(size(J1,1),1)" to "NaN(size(y1))" since size(J1,1) = size(obj.dy,1) + 1 for conservative systems (line 41)
        - SOL_PS_SHM:
            --> Changed the initialisation of obj.dy from "NaN(size(J1,1),1)" to "NaN(size(y1))" since size(J1,1) = size(obj.dy,1) + 1 for conservative systems (line 30)
    
    Functions:
        - initial_solution:
            --> The fsolve algorithm is set to Levenberg-Marquardt if the DYN.system field 'first_integral' exists (lines 24 - 26)
            --> Added re-computation and check of residuum after fsolve. A residuum >= 1e-10 is treated like fsolve exitflag < 1 or = 2 (lines 79, 83, 84 and 91 - 93)

    test:
        - test_FDM_PS_pendulum
            --> Added to test continuation via value of first integral using the FDM
        - test_FGM_PS_pendulum
            --> Added to test continuation via value of first integral using the FGM
        - test_SHM_PS_pendulum
            --> Added to test continuation via value of first integral using the SHM

    costar:
        --> Added DYN as input argument to the Continuation class (line 138)


----------  

Bugfixes:

    @Continuation:
        - Continuation:
            --> Changed the initialisation of the property p_newton_flag from 0 to 1i (line 34)
        - m_continuation:
            --> if-condition for execution of method "direction_vector": Changed the 0 to 1i to circumvent the method if obj.p_newton_flag = 0 (line 56 and 60)
            --> Added an "is" to error_text for fsolve exitflag = 2 (line 105)

    @AM_PS_SHM:
        - getIV:
            --> Moved the elseif-condition "numel(s0) == dim*n_shoot" to the top so that s0 is also directly used as obj.iv for n_shoot = 1
                (prior to this, the first block belonging to condition "numel(s0) == dim" was also executed for n_shoot = 1, although an integration is not necessary in this case)
        - PS_SHM_residuum:
            --> Comment in line 96 corrected: "[dim x (2*dim+2)]" instead of "[dim x (2*dim+1)]"

    Functions:
        - initial_solution:
            --> Added an "is" to error_text for fsolve exitflag = 2 (line 89)


----------  

@Continuation:
        
    - check_limits:
        --> "<" and ">" at the checks if mu is outside the limits changed to "<=" and ">=" (lines 62 and 70)
    - choose_stepcontrol_param:
        --> Value of "it_nom" changed from 2 to 3 for step control method 'angle' (line 27)
    - initial_slope:
        --> if-condition that checks failure modified: "secant_flag == 2" removed since the Jacobian is not needed here (line 25)
    - m_continuation:
        --> Adapted the warn_text and info_text messages if fsolve did not converge (lines 121, 123, 124 and 137)


Functions:

    - initial_solution:
        --> Moved the code to check the Jacobian matrix from line 76 to line 116 and added the comment in line 115 (now it is almost identical to m_continuation)
